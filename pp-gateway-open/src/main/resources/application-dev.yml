spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project?characterEncoding=utf8&useSSL=false&verifyServerCertificate=false&serverTimezone=UTC
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      pool-name: KevinHikariPool
      maximum-pool-size: 12
      connection-timeout: 60000
      minimum-idle: 10
      idle-timeout: 50000
      max-lifetime: 540000
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5672
    listener:
      direct:
        acknowledge-mode: manual
      simple:
        acknowledge-mode: manual